# This directory contains services.json — the persistent service registry for ServiceAPI.
# services.json is excluded from version control via .gitignore.
# The file is auto-created and seeded with default services on first module load.
# Delete services.json before sharing the module outside your team to remove environment-specific URLs.
